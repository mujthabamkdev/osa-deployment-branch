# Railway monorepo configuration
# IMPORTANT: Deploy each service separately by pointing Railway to the subdirectories
# Do NOT deploy from the root directory

# This file is for reference only - create separate Railway services:
# 1. Backend service: Set Root Directory to "osa-backend"
# 2. Frontend service: Set Root Directory to "osa-frontend"
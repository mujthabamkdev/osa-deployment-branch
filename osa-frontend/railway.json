{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build:prod && npm install -g serve"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "serve dist/osa-frontend -s -l $PORT -n",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "cronSchedule": ""
  }
}